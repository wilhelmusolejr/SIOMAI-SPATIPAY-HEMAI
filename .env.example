PORT=3001
# The brand/product term itself (e.g. "Siomai"), used as the logo label.
# Must be VITE_ prefixed so Vite exposes it to the browser.
VITE_BRAND_TERM=Brand
# Full brand name/label shown in the website header and browser tab.
VITE_BRAND_NAME=Brand Redemption
SERVICE_TOKEN=
PROXY_USERNAME=
PROXY_PASSWORD=
PROXYSCRAPE_API_TOKEN=
